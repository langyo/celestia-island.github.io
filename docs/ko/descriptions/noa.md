# Noa

AI 네이티브 분산 버전 관리 시스템 — 소스 코드는 git, AI 에이전트 이터레이션 데이터는 noa.

동일 작업 트리에서 `.git`과 공존. 에이전트별 추가 전용 JSONL 로그(제로 락)로 수백 개의 에이전트가 동시에 쓰기 가능. 스냅샷 기반 불변 히스토리가 전체 이터레이션 추적을 보존. 파일 수준 충돌 감지를 갖춘 워크스페이스 3-way 병합. 시스템 `git` CLI 브릿지를 통한 완전한 git 프로토콜 호환성. 크로스 플랫폼 (Linux, macOS), crates.io에 `libnoa`로 게시.
