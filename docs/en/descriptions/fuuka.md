# Fuuka

Run GitHub Actions on your own machines

A self-hosted CI execution plane: it picks up queued GitHub Actions jobs on your own nodes, runs every step inside an isolated container sandbox, collects the artifacts, and deploys the built binaries to the target hosts.
