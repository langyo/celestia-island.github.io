# seia

A **multi-engine web search** library and CLI — one query, many backends, unified results.

seia offers a single interface to a growing roster of search providers. Engines that need no authentication work out of the box with zero configuration, so you can route a query across several backends and merge the results without writing a per-engine client each time.
