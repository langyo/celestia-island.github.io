# Hikari

A **WASI-native** UI component library — polished, ready-made components that run on the same WASI runtime as your backend.

Built on top of Tairitsu, Hikari provides a comprehensive set of UI components without the overhead of `wasm-bindgen` or JavaScript glue code. Build interfaces once, render them anywhere the WASI runtime goes: browser, server, or edge.
