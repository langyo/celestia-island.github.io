# Lagrange

A Markdown static site facility — multilingual out of the box.

Hand Lagrange a folder of Markdown and it returns a static, multilingual documentation site with a built-in language switcher and root redirect. No JavaScript framework, no mdBook, no Node toolchain. It renders its own documentation — the docs you publish were produced by Lagrange itself.
