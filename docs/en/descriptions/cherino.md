# Cherino

Sandboxed containers behind one trait

A container sandbox toolkit: one ContainerOps trait with Docker as the primary backend, rootless youki as the fallback, and experimental CLI backends — security profiles, AppArmor, seccomp, and egress policy built in.
