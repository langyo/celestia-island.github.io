# shirabe

A lightweight **Rust-native browser automation** library and debug server.

shirabe drives any browser that speaks the Chrome DevTools Protocol — Chrome, Chromium, Edge — through one hand-rolled CDP engine. A pinned Chrome for Testing build is fetched into a shared cache and located transparently, so you never install a browser by hand. Think ort for ONNX Runtime, but for a headless browser.
