# Noa

AI 原生的分散式版本控制系統 — 原始碼交給 git，AI 智能體的迭代資料交給 noa。

與 `.git` 在同一工作樹中共存。每個智能體獨立的追加寫入 JSONL 日誌（零鎖），支援數百個智能體並發寫入。基於快照的不可變歷史，完整保留迭代軌跡。工作區三路合併，帶檔案級衝突檢測。透過系統 `git` CLI 橋接實現完整的 git 協定相容。跨平台（Linux、macOS），以 `libnoa` 發佈在 crates.io。
