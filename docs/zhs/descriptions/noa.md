# Noa

AI 原生的分布式版本控制系统 — 源代码交给 git，AI 智能体的迭代数据交给 noa。

与 `.git` 在同一工作树中共存。每个智能体独立的追加写入 JSONL 日志（零锁），支持数百个智能体并发写入。基于快照的不可变历史，完整保留迭代轨迹。工作区三路合并，带文件级冲突检测。通过系统 `git` CLI 桥接实现完整的 git 协议兼容。跨平台（Linux、macOS），以 `libnoa` 发布在 crates.io。
