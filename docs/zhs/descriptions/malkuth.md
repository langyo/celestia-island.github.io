# Malkuth

面向长期运行 Rust 程序的可组合**服务监管工具包**。

Malkuth 接手生产服务里那些不起眼却要命的环节——优雅关闭、健康探针、协调锁、滚动更新——并通过可插拔的传输（基于 TCP、WebSocket 或 IPC 的 JSON-RPC）来承载。受监管的工作进程、drain 钩子，以及一个用文件监视与粘性反向代理包裹任意程序的 watchdog CLI，把监管能力带到任何你能运行的程序之上。
