# Yuuka

Runtime de merge/patch JSON et macros de structures imbriquées

Une bibliothèque de base merge/patch JSON — merge patch RFC 7396, patchs d'opérations set/replace/del, diffs et instantanés de viewport — et des macros qui construisent des structures profondément imbriquées à partir d'un DSL compact.
